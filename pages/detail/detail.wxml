<!--index.wxml-->
<view class="container">
  <view class="search appbar">
    <i class="material-icons">back</i>
    <view class="brand">{{ loading ? 'loading' : entry.name }}</view>
  </view>
</view>
